---
properties:
  code:
    description: "The code the user, group or organization that will be mentioned.\n\
      The maximum number of mentions is 10.\nThe mentioned users will be placed in\
      \ front of the comment text when the API succeeds.\n"
    type: "string"
  type:
    description: "The type of the mentioned target.\n- USER: User\n- GROUP: Group\n\
      - ORGANIZATION: Department\n"
    enum:
    - "USER"
    - "ORGANIZATION"
    - "GROUP"
    type: "string"
required:
- "code"
- "type"
type: "object"

