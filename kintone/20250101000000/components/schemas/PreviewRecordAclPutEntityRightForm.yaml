---
properties:
  deletable:
    format: "boolean"
    type: "boolean"
  editable:
    format: "boolean"
    type: "boolean"
  entity:
    properties:
      code:
        type: "string"
      type:
        enum:
        - "USER"
        - "GROUP"
        - "ORGANIZATION"
        - "FIELD_ENTITY"
        type: "string"
    required:
    - "type"
    type: "object"
  includeSubs:
    format: "boolean"
    type: "boolean"
  viewable:
    format: "boolean"
    type: "boolean"
required:
- "entity"
type: "object"

