---
properties:
  commentAdded:
    format: "boolean"
    type: "boolean"
  entity:
    properties:
      code:
        type: "string"
      type:
        enum:
        - "USER"
        - "GROUP"
        - "ORGANIZATION"
        - "FIELD_ENTITY"
        type: "string"
    required:
    - "code"
    - "type"
    type: "object"
  fileImported:
    format: "boolean"
    type: "boolean"
  includeSubs:
    format: "boolean"
    type: "boolean"
  recordAdded:
    format: "boolean"
    type: "boolean"
  recordEdited:
    format: "boolean"
    type: "boolean"
  statusChanged:
    format: "boolean"
    type: "boolean"
required:
- "entity"
type: "object"

