---
properties:
  dayOfMonth:
    format: "long"
    type: "integer"
  every:
    enum:
    - "YEAR"
    type: "string"
  month:
    format: "long"
    type: "integer"
  time:
    type: "string"
type: "object"

