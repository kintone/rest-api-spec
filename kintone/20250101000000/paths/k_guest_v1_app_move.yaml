---
post:
  operationId: "postAppMoveForGuestSpace"
  parameters:
  - description: "guest space id"
    in: "path"
    name: "guestSpaceId"
    required: true
    schema:
      type: "integer"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          properties:
            app:
              format: "long"
              type: "integer"
            space:
              format: "long"
              type: "integer"
          required:
          - "app"
          title: "postAppMoveForGuestSpaceRequest"
          type: "object"
  responses:
    "200":
      description: "success case of app/move/post"
      content:
        application/json:
          schema: {}
  tags:
  - "APP"

