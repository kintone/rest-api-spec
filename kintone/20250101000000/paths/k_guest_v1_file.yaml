---
get:
  operationId: "getFileForGuestSpace"
  parameters:
  - description: "guest space id"
    in: "path"
    name: "guestSpaceId"
    required: true
    schema:
      description: "guest space id"
      title: "guestSpaceId"
      type: "integer"
  - in: "query"
    name: "fileKey"
    required: true
    schema:
      type: "string"
  responses:
    "200":
      description: "success case of file/get"
  tags:
  - "FILES"
post:
  operationId: "postFileForGuestSpace"
  parameters:
  - description: "guest space id"
    in: "path"
    name: "guestSpaceId"
    required: true
    schema:
      description: "guest space id"
      title: "guestSpaceId"
      type: "integer"
  requestBody:
    required: true
    content:
      multipart/form-data:
        schema:
          properties:
            file: {}
          title: "postFileForGuestSpaceRequest"
          type: "object"
  responses:
    "200":
      description: "success case of file/post"
      content:
        application/json:
          schema:
            properties:
              fileKey:
                type: "string"
            title: "postFileForGuestSpaceResponse"
            type: "object"
  tags:
  - "FILES"

